import Ticker from "./ticker.svelte";
import TickerIcon from "./ticker-icon.svelte";
import TickerPrice from "./ticker-price.svelte";
import TickerPriceChange from "./ticker-price-change.svelte";
import TickerSymbol from "./ticker-symbol.svelte";

export {
    Ticker,
    TickerIcon,
    TickerPrice,
    TickerPriceChange,
    TickerSymbol,

    Ticker as Root,
    TickerIcon as Icon,
    TickerPrice as Price,
    TickerPriceChange as PriceChange,
    TickerSymbol as Symbol
};
